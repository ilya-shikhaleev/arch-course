apiVersion: v1
kind: Service
metadata:
  name: {{ include "cart-chart.fullname" . }}
  labels:
    {{- include "cart-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      protocol: TCP
      targetPort: {{ .Values.appPort }}
      name: http
  selector:
    {{- include "cart-chart.selectorLabels" . | nindent 4 }}
