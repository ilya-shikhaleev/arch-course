apiVersion: v1
kind: Service
metadata:
  name: {{ include "user-info-chart.fullname" . }}
  labels:
    {{- include "user-info-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      protocol: TCP
      targetPort: {{ .Values.appPort }}
      name: http
  selector:
    {{- include "user-info-chart.selectorLabels" . | nindent 4 }}
