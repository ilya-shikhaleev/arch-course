apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "user-info-chart.fullname" . }}-test-connection"
  labels:
    {{- include "user-info-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "user-info-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
